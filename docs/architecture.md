# Architecture

Task 1 establishes the deterministic evidence primitives that later receipt, signature, and validation steps will depend on:

- canonical JSON serialization
- stable SHA-256 hashing
- typed request, policy, manifest, and tool-output models

No runtime flow, signing, or validation logic is implemented in this phase.
